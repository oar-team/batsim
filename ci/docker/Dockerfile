# This docker container is not built from this Dockerfile.
# It is built by loading a container tarball generated by Nix.
# nix-build -A batsim-docker-master https://github.com/oar-team/nur-kapack/archive/master.tar.gz
FROM scratch
